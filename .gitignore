# Build and local tooling
dist/
*.luac
node_modules/

# Editors
.idea/
*.swp
.DS_Store

# Local server configuration: contains credentials
server.jsonc
acl.jsonc
.env

# Agent tooling: 16 MB of skills, two of them nested git repos
.claude/

# Dev only. Generated by tools/, never shipped and never pushed: the generator
# belongs in the repository, its output is one command away, and the stills it
# embeds are somebody else's screenshots.
preview.html
tools/preview-bg.jpg
